# Gantt Chart Application

Приложение для управления задачами с визуализацией в виде Gantt-диаграммы, календаря и аналитики.

## 🚀 Технологии

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL/SQLite
- **Frontend**: React, Material-UI (MUI)
- **Аутентификация**: JWT токены
- **База данных**: PostgreSQL (продакшн) или SQLite (разработка)

## 📋 Функционал

### Управление задачами
- ✅ Создание задач с поддержкой двух режимов:
  - **По месяцам**: выбор из доступных месяцев (Декабрь, Январь, Февраль, Март, Апрель, Май, Июнь, Июль)
  - **По дням**: точные даты начала и окончания с указанием времени
- ✅ Добавление подзадач (наследуют длительность основной задачи)
- ✅ Редактирование и удаление задач
- ✅ Отметка о выполнении задач
- ✅ Автодополнение проектов (тегов)

### Визуализация
- 📊 **Gantt-диаграмма**: визуализация задач по месяцам с поддержкой коротких задач (меньше месяца)
- 📅 **Календарный вид**: отображение задач по дням с уникальными цветами
- 📈 **Дашборд**: аналитика и статистика:
  - Общее количество задач, подзадач, проектов
  - Распределение по месяцам и проектам
  - Повторяющиеся задачи (2+ месяца)
  - Эффективность выполнения (в срок, заранее, просрочено)
  - Рекомендации по повышению эффективности

### Экспорт
- 📄 Экспорт Gantt-диаграммы в PDF
- 📊 Экспорт календаря в PDF
- 📋 Экспорт задач в CSV (с форматированием для Excel)

### Управление пользователями (только для супер-админа)
- 👥 Просмотр всех пользователей
- ➕ Создание новых пользователей
- ✏️ Редактирование пользователей
- 🔑 Сброс паролей
- 🗑️ Удаление пользователей
- 👁️ Просмотр задач других пользователей

### Аутентификация
- 🔐 JWT токены
- 👤 Роли: обычный пользователь и супер-админ
- ⚙️ Настройки профиля (изменение логина и пароля)

## 🛠️ Установка и запуск

### Требования
- Python 3.9+
- Node.js 14+
- PostgreSQL (опционально, для продакшена)

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. (Опционально) Настройте PostgreSQL:
   - Создайте базу данных
   - Создайте файл `.env` с `DATABASE_URL="postgresql://user:password@localhost/gantt_db"`
   - Или используйте SQLite по умолчанию (для разработки)

4. Запустите сервер:
```bash
python main.py
```

Backend будет доступен на http://localhost:8001

**Данные для входа супер-админа:**
- Email: `admin@admin.ru`
- Пароль: `admin123`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение:
```bash
npm start
```

Frontend будет доступен на http://localhost:3000

## 📁 Структура проекта

```
Gant/
├── backend/
│   ├── main.py              # FastAPI приложение
│   ├── auth.py              # Аутентификация
│   ├── database.py          # Модели базы данных
│   ├── models.py            # Pydantic модели
│   ├── db_helpers.py        # Вспомогательные функции
│   ├── requirements.txt     # Python зависимости
│   └── README_POSTGRES.md   # Инструкции по PostgreSQL
├── frontend/
│   ├── src/
│   │   ├── App.js           # Главный компонент
│   │   ├── components/
│   │   │   ├── TaskForm.js      # Форма создания/редактирования задач
│   │   │   ├── GanttChart.js    # Gantt диаграмма
│   │   │   ├── CalendarView.js  # Календарный вид
│   │   │   ├── Dashboard.js     # Дашборд с аналитикой
│   │   │   ├── Login.js         # Форма входа
│   │   │   ├── UserManagement.js # Управление пользователями
│   │   │   └── Settings.js      # Настройки профиля
│   │   └── index.js
│   └── package.json
└── README.md
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получить информацию о текущем пользователе

### Задачи
- `GET /api/tasks` - Получить все задачи (с фильтром по user_id для супер-админа)
- `GET /api/tasks/{task_id}` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/{task_id}` - Обновить задачу
- `DELETE /api/tasks/{task_id}` - Удалить задачу
- `GET /api/tasks/export/csv` - Экспорт в CSV

### Пользователи (только для супер-админа)
- `GET /api/users` - Получить всех пользователей
- `GET /api/users/{user_id}` - Получить пользователя по ID
- `POST /api/users` - Создать пользователя
- `PUT /api/users/{user_id}` - Обновить пользователя
- `DELETE /api/users/{user_id}` - Удалить пользователя
- `POST /api/users/{user_id}/reset-password` - Сбросить пароль

## 🎨 Особенности UI

- **Bento UI стиль**: современный дизайн с градиентами и скругленными углами
- **Адаптивная верстка**: работает на всех устройствах
- **Темная тема**: для Gantt диаграммы и календаря
- **Интерактивность**: редактирование, удаление, фильтрация прямо из интерфейса

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Разработано для управления проектами и задачами.
